[project]
name = "homelab-ansible"
version = "0.1.0"
description = "Ansible environment for homelab management"
requires-python = "==3.14.*"
dependencies = [
    "ansible-core==2.20.6",
    "ansible-lint>=25.0.0",
    "yamllint>=1.35.0",
    "paramiko>=3.5.0",
]

[tool.uv]
package = false
