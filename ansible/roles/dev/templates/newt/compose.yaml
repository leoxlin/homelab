---
name: newt

services:
  newt:
    image: fosrl/newt
    container_name: newt
    restart: unless-stopped

    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

    env_file:
      - .env
