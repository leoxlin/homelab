---
k3s_cluster_cidr: "{{ (k3s_server_config_yaml | from_yaml)['cluster-cidr'] | default('10.42.0.0/16') }}"
k3s_service_cidr: "{{ (k3s_server_config_yaml | from_yaml)['service-cidr'] | default('10.43.0.0/16') }}"
api_endpoint: "{{ hostvars[groups[k3s_server_group][0]]['ansible_host'] | default(groups[k3s_server_group][0]) }}"
