---
apiVersion: v1
kind: Service
metadata:
  name: cloudcli
spec:
  selector:
    app: cloudcli
  ports:
    - port: 80
      targetPort: 4180
      name: http
---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: cloudcli
spec:
  hosts:
    - code.hydrahmlb.cc
  gateways:
    - istio-ingress/hydrahmlb-gateway
  http:
    - match:
        - port: 80
      redirect:
        scheme: https
        redirectCode: 301
    - route:
        - destination:
            host: cloudcli.default.svc.cluster.local
            port:
              number: 80
