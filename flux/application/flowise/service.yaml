---
apiVersion: v1
kind: Service
metadata:
  name: flowise
spec:
  selector:
    app: flowise
  ports:
    - port: 3000
      targetPort: 3000
      name: http
---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: flowise
spec:
  hosts:
    - flowise.hydrahmlb.cc
  gateways:
    - istio-ingress/hydrahmlb-gateway
  http:
    - match:
        - port: 80
      redirect:
        scheme: https
        redirectCode: 301
    - route:
        - destination:
            host: flowise.default.svc.cluster.local
            port:
              number: 3000
